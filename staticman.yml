allowedFields          : ['name', 'email', 'message']
branch                 : "master"
commitMessage          : "New staticman comment from {fields.name}"
filename               : "comment-{fields.name}-{@timestamp}"
format                 : "yml"
moderation             : true
path                   : "_data/comments/{options.slug}"
requiredFields         : ['name', 'message']
generatedFields:
  data:
    type               : "date"
    options:
      format           : "iso8601"
